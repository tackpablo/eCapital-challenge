import * as React from "react";
import EmployeeList from "./EmployeeList";

const HomePage = () => {
    return (
        <div>
            <EmployeeList />
        </div>
    );
};

export default HomePage;
